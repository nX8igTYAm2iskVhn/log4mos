source 'http://rubygems.org'

gemspec

gem 'request_context', '0.1.2' # internal

group :test do
  gem 'rspec'
  gem 'timecop'
  gem 'simplecov'
  gem 'pry'
  gem 'rake' # avoiding nX8igTYAm2iskVhn CI error "Gems in the groups production and development were not installed. rake is not part of the bundle. Add it to Gemfile"
end
